//A local function/variable in this file will not be accessible to other files
var circleArea = function(radius) {
	return Math.pow(radius, 2) * Math.PI;
}

var boxVolume = function(length, width, height) {
	return length * width * height;
}

//One way to export function/variable is by assigning a "public variable"
//to the chosen function/variable
exports.circle_Area = circleArea;
exports.box_Volume = boxVolume;

//Another way to export
module.exports {
	testArea : (width) => {
		return Math.pow(width, 2)
	}
}